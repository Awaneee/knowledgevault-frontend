import 'package:flutter/material.dart';

abstract final class AppColors {
  // Primary brand — rich indigo
  static const seedColor = Color(0xFF4F46E5);

  // Accent surfaces used in custom widgets
  static const userBubble = Color(0xFF4F46E5);
  static const userBubbleText = Colors.white;
  static const assistantBubble = Color(0xFFF1F0FF);
  static const assistantBubbleDark = Color(0xFF1E1B4B);

  static const success = Color(0xFF059669);
  static const warning = Color(0xFFD97706);
}
